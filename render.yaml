services:
  # API Service
  - type: web
    name: neurai-api
    env: docker
    region: oregon
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
      - key: MODEL_PATH
        value: ./processed/trained_model.h5
      - key: LOG_LEVEL
        value: INFO
      - key: MAX_WORKERS
        value: 4
    disk:
      name: neurai-data
      mountPath: /app/data
      sizeGB: 10 